# Python
__pycache__/
*.pyc
.venv/
venv/

# Logs and ephemeral run output
*.log

# Secrets (never commit real credentials)
.env
.env.local

# Raw source shapefiles (~90MB of TIGER data) -- not needed at runtime,
# only to regenerate geo_assets/ via build_geo_assets.py. geo_assets/
# itself (the simplified GeoJSON actually served to the map) IS tracked --
# at ~10MB it's small enough to commit, and deployed environments need it
# without also needing these raw shapefiles.
Clean Shapefiles/
PUMA/Shapefiles/

# Frontend (added once scaffolded)
node_modules/
dist/
build/

# Claude Code local tool state -- machine-specific, not project source
.claude/
